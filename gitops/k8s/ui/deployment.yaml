apiVersion: apps/v1
kind: Deployment
metadata:
  name: &Name ui-deployment
spec:
  selector:
    matchLabels:
      app: *Name
  replicas: 1
  template:
    metadata:
      labels:
        app: *Name
    spec:
      containers:
        - name: ui-deployment-container
          image: samhumphreys/code:f57b6367a100b5f1ed73c0d6a3455ffadb2e9dcf
          env:
            - name: USERNAME
              valueFrom:
                secretKeyRef:
                  name: ui-creds
                  key: username
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ui-creds
                  key: password
            - name: RECIPIENT
              valueFrom:
                secretKeyRef:
                  name: ui-creds
                  key: recipient
          # Deploy with gunicorn due to Flask server intended only for development
          command: ["gunicorn"]
          args: ["-b", "0.0.0.0:5000", "code.ui:app"]
          resources:
            limits:
              cpu: 100m
              memory: 500Mi
          ports:
            - name: http
              containerPort: 5000
      volumes:
        - name: ui-creds
          secret:
            secretName: ui-creds